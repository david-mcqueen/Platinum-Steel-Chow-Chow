class SceneMain extends Phaser.Scene {
    constructor() {
        super('SceneMain'); // Pass the name so we can refer to it later
    }

    preload() {
        // Load images or resources
    }

    create() {
        // define our objects
    }

    update() {
        // runs every tick - the main func
    }
}


export default SceneMain;